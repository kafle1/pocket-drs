\documentclass[12pt,a4paper]{report}

\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{microtype}
\usepackage[top=0.9in,bottom=0.9in,left=1.2in,right=1in]{geometry}
\renewcommand{\rmdefault}{ptm}
\usepackage{setspace}
\usepackage{graphicx}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{array}
\usepackage{float}
\usepackage{caption}
\usepackage{hyperref}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{listings}
\usepackage{xcolor}

\onehalfspacing
\setlength{\parindent}{0pt}
\setlength{\parskip}{3pt}
\renewcommand{\arraystretch}{0.95}
\setlength{\LTpre}{4pt}
\setlength{\LTpost}{4pt}

% Heading formatting per syllabus: chapter 16pt (centred), section 14pt,
% subsection 12pt, all bold
\titleformat{\chapter}[display]
  {\normalfont\fontsize{16}{19}\bfseries\centering}
  {\MakeUppercase{\chaptertitlename}\ \thechapter}
  {0.6em}
  {}
\titlespacing*{\chapter}{0pt}{0pt}{24pt}
\titleformat{\section}
  {\normalfont\fontsize{14}{17}\bfseries}{\thesection}{1em}{}
\titleformat{\subsection}
  {\normalfont\fontsize{12}{15}\bfseries}{\thesubsection}{1em}{}
\titleformat{\subsubsection}
  {\normalfont\fontsize{12}{15}\bfseries}{\thesubsubsection}{1em}{}

% Captions: bold, 12pt, centred (figures below, tables above)
\captionsetup{font=bf,labelsep=colon,justification=centering,singlelinecheck=false}
\renewcommand{\contentsname}{Table of Contents}
\renewcommand{\listtablename}{List of Tables}
\renewcommand{\listfigurename}{List of Figures}
\renewcommand{\bibname}{References}

\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0pt}
\fancypagestyle{plain}{\fancyhf{}\fancyfoot[C]{\thepage}\renewcommand{\headrulewidth}{0pt}}

\hypersetup{
  colorlinks=true,
  linkcolor=black,
  citecolor=black,
  urlcolor=blue,
  pdftitle={PocketDRS Final Report},
  pdfauthor={Niraj Kafle}
}

\lstset{
  basicstyle=\small\ttfamily,
  breaklines=true,
  showstringspaces=false,
  keywordstyle=\color{blue},
  commentstyle=\color{gray},
  stringstyle=\color{red},
  numbers=left,
  numberstyle=\tiny,
  columns=flexible
}

\begin{document}

\hypersetup{pageanchor=false}

% =====================================================================
% TITLE PAGE
% =====================================================================
\begin{titlepage}
\begin{center}

\textbf{\fontsize{14}{17}\selectfont Lincoln University College}\\[4pt]
\textbf{\fontsize{12}{15}\selectfont Faculty of Computer Science and Multimedia}\\[28pt]

\textbf{\fontsize{16}{19}\selectfont PocketDRS: A Single-View 3D Trajectory\\
Reconstruction and Decision Review System for Cricket}\\[24pt]

\textbf{\fontsize{14}{17}\selectfont FINAL REPORT}\\[10pt]
\fontsize{12}{15}\selectfont
Submitted to\\[4pt]
Department of Information Technology\\
Phoenix College of Management\\
Maitidevi, Kathmandu\\[20pt]

In partial fulfillment of the requirements for the degree of\\
\textbf{Bachelor in Information Technology (BIT)}\\[24pt]

Submitted by\\[8pt]
\textbf{Niraj Kafle}\\
BIT 7th Semester\\
University ID: LC0003001674\\[22pt]

Under the Supervision of\\[6pt]
\textbf{Mr. Saishab Bhattarai}\\
Project Supervisor\\[24pt]

\vfill
\textbf{May, 2026}

\end{center}
\end{titlepage}

\pagenumbering{roman}

% =====================================================================
% DECLARATION
% =====================================================================
\begin{center}\textbf{\fontsize{16}{19}\selectfont DECLARATION}\end{center}
\addcontentsline{toc}{chapter}{Declaration}
\vspace{0.5cm}

I hereby declare that this report is an original work prepared by me for the requirement of the Bachelor in Information Technology programme under the supervision of Mr. Saishab Bhattarai. All sources and references used have been duly acknowledged. The report has not been previously submitted to any institution for any degree or diploma.

\vspace{1.5cm}

\noindent
Date: \underline{\hspace{4cm}}

\noindent
Signature: \underline{\hspace{4cm}}

\noindent
Name: Niraj Kafle

\clearpage

% =====================================================================
% LETTER OF APPROVAL
% =====================================================================
\begin{center}\textbf{\fontsize{16}{19}\selectfont LETTER OF APPROVAL}\end{center}
\addcontentsline{toc}{chapter}{Letter of Approval}
\vspace{0.5cm}

This is to certify that the following student has completed the Final Report as required by the Bachelor in Information Technology programme.

\vspace{1.5cm}

\noindent
\textbf{Project Title:} PocketDRS: A Single-View 3D Trajectory Reconstruction and Decision Review System for Cricket

\noindent
\textbf{Student Name:} Niraj Kafle

\noindent
\textbf{University ID:} LC0003001674

\vspace{2cm}

\noindent
\textbf{Project Supervisor:} Mr. Saishab Bhattarai

\noindent
Date: \underline{\hspace{4cm}}

\noindent
Signature: \underline{\hspace{4cm}}

\vspace{2.5cm}

\noindent
\textbf{Head of Department}

\noindent
Date: \underline{\hspace{4cm}}

\noindent
Signature: \underline{\hspace{4cm}}

\clearpage

% =====================================================================
% ABSTRACT
% =====================================================================
\begin{center}\textbf{\fontsize{16}{19}\selectfont ABSTRACT}\end{center}
\addcontentsline{toc}{chapter}{Abstract}
\vspace{0.5cm}

PocketDRS is a low-cost Leg Before Wicket (LBW) decision-support system for cricket that works with a single smartphone camera. Professional systems such as Hawk-Eye need six to eight synchronised high-speed cameras and dedicated hardware, which puts them out of reach of schools, clubs, and training grounds. PocketDRS shows that a careful single-view workflow can still produce practical decision support at those venues. A Flutter app records the delivery and lets the user tap the four pitch corners. A FastAPI server then detects the ball from motion and colour, links the detections into one smooth path using RANSAC, recovers the camera pose from the tapped corners, and rebuilds the ball trajectory in real-world 3D using projectile-motion physics. A rule-based engine applies the three LBW checks of ICC Rule~36. On 66 simulated deliveries the system recovered the camera position correctly every time and returned the right verdict in 53 cases (80.3\%), including all 16 OUT cases.

\vspace{1cm}

\noindent
\textbf{Keywords:} Cricket, Decision Review System, Monocular 3D Reconstruction, Computer Vision, LBW.

\clearpage

% =====================================================================
% ACKNOWLEDGEMENT
% =====================================================================
\begin{center}\textbf{\fontsize{16}{19}\selectfont ACKNOWLEDGEMENT}\end{center}
\addcontentsline{toc}{chapter}{Acknowledgement}
\vspace{0.5cm}

I would like to express my sincere gratitude to all those who contributed to the completion of this project. Their guidance and continuous support have been invaluable throughout this work.

I am profoundly grateful to the Principal of Phoenix College of Management, Prof.~Dr.~Fatta Bahadur KC, for his inspiring leadership and academic guidance.

My deepest appreciation goes to my project supervisor, Mr.~Saishab Bhattarai, for his invaluable guidance, constructive feedback, and continuous support. His expertise and insightful suggestions greatly contributed to the success of this work.

I am also thankful to the Department of Information Technology at Phoenix College of Management for the academic environment and institutional support, and to the faculty members for their suggestions during the course of this work.

Finally, I express my deepest gratitude to my family and friends for their unwavering support, motivation, and encouragement throughout this journey.

\clearpage

% =====================================================================
% CERTIFICATE
% =====================================================================
\begin{center}\textbf{\fontsize{16}{19}\selectfont CERTIFICATE}\end{center}
\addcontentsline{toc}{chapter}{Certificate}
\vspace{0.5cm}

This is to certify that the student Niraj Kafle has completed the Bachelor in Information Technology Final Year Project titled ``PocketDRS: A Single-View 3D Trajectory Reconstruction and Decision Review System for Cricket'' under the supervision of Mr. Saishab Bhattarai at Lincoln University College, Faculty of Computer Science and Multimedia. The student has successfully implemented a working prototype, conducted testing, and documented the system according to academic standards. This certificate is issued on completion of all project requirements.

\vspace{3cm}

Issued at: Phoenix College of Management, Maitidevi, Kathmandu

Date: May, 2026

\clearpage
\hypersetup{pageanchor=true}

% =====================================================================
% TABLE OF CONTENTS / LISTS
% =====================================================================
\tableofcontents

\addcontentsline{toc}{chapter}{List of Figures}
\listoffigures

\addcontentsline{toc}{chapter}{List of Tables}
\listoftables
\clearpage

% =====================================================================
% LIST OF ABBREVIATIONS
% =====================================================================
\begin{center}\textbf{\fontsize{16}{19}\selectfont LIST OF ABBREVIATIONS}\end{center}
\addcontentsline{toc}{chapter}{List of Abbreviations}
\vspace{0.5cm}

\begin{tabularx}{\textwidth}{lX}
API & Application Programming Interface \\
BIT & Bachelor in Information Technology \\
CV & Computer Vision \\
DRS & Decision Review System \\
DFD & Data Flow Diagram \\
EKF & Extended Kalman Filter \\
ER & Entity-Relationship \\
FOV & Field of View \\
FPS & Frames Per Second \\
HSV & Hue, Saturation, Value \\
ICC & International Cricket Council \\
LBW & Leg Before Wicket \\
MOG2 & Mixture of Gaussians 2 \\
PnP & Perspective-n-Point \\
RANSAC & Random Sample Consensus \\
RMS & Root Mean Square \\
ROI & Region of Interest \\
SDK & Software Development Kit \\
UI/UX & User Interface / User Experience \\
UML & Unified Modeling Language \\
\end{tabularx}

\clearpage
\pagenumbering{arabic}

% =====================================================================
% CHAPTER 1: INTRODUCTION
% =====================================================================
\chapter{Introduction}

\section{Introduction}

Modern cricket relies heavily on technology to review close decisions, and Leg Before Wicket (LBW) is one of the hardest of those decisions for the human eye. At the top level the game uses Hawk-Eye, which reconstructs the ball trajectory from six to eight synchronised high-speed cameras and dedicated processing hardware. Hawk-Eye is accurate, but the equipment, installation, and operating cost place it well beyond the reach of schools, clubs, local leagues, and coaching grounds, where the same close decisions occur every week with no review support at all.

PocketDRS addresses this gap. It is a decision-support system that runs on a normal smartphone and a lightweight cloud service. The user records or picks a short clip of the delivery, taps the four corners of the visible pitch to calibrate, sends the clip to the server, and gets back a 3D visualisation together with an LBW verdict. The aim is practical decision support at low cost, not broadcast-grade precision; the system is therefore positioned as an aid to umpiring rather than a replacement for it.

The contribution of the project is to show that a carefully constrained single-camera pipeline, built around the known geometry of a cricket pitch and the physics of projectile motion, can produce consistent and human-readable LBW review support end-to-end. The working prototype demonstrates this with a complete mobile-to-cloud system that combines computer vision, geometric reconstruction, and cricket-specific decision logic.

\section{Problem Statement}

Professional DRS systems depend on multiple synchronized cameras to recover depth and reconstruct the ball trajectory in three dimensions. This produces strong results, but the required equipment, installation effort, and operating cost make such systems inaccessible outside major venues.

When the same decision-review problem is shifted to a single consumer camera, several technical and practical difficulties arise:

\begin{enumerate}
    \item A monocular view does not provide direct depth information, requiring alternative cues such as ball size or strong motion constraints.
    \item A cricket ball is small, fast, motion-blurred, and can easily be confused with background objects, players, shadows, and spectators.
    \item Small calibration errors can produce large errors when detections are mapped to pitch coordinates, especially at the far end where perspective foreshortening is strong.
    \item Lighting variation, camera shake, player occlusion, and inconsistent recording conditions reduce reliability in real grounds compared to controlled broadcast environments.
\end{enumerate}

Therefore, the central problem addressed by this project is whether a single-phone-camera workflow, supported by pitch calibration and lightweight computer-vision techniques, can produce a sufficiently consistent and understandable LBW review aid for ordinary cricket grounds.

\section{Objectives}

\subsection{General Objective}

To design and implement a low-cost cricket decision-support system that uses a single camera view to analyze a delivery, estimate the ball path in calibrated pitch coordinates, and assist LBW review.

\subsection{Specific Objectives}

\begin{enumerate}
    \item Study existing decision-review systems and relevant literature on monocular sports tracking.
    \item Develop a user-guided calibration process based on pitch corners and stump reference points.
    \item Detect and track the ball from delivery video using combined motion and colour cues.
    \item Map tracked pixel positions to pitch-plane coordinates using homography.
    \item Detect important events such as bounce and impact from the tracked trajectory using physics-based fitting.
    \item Estimate an LBW outcome using rule-based checks on pitching line, impact line, and projected stump contact.
    \item Present the result through an interactive 3D visualization that helps users interpret the decision.
\end{enumerate}

\section{Scope and Limitation}

\subsection{Scope}

The PocketDRS project encompasses the complete workflow from video capture to LBW decision:
\begin{itemize}
    \item Mobile application for video selection, trimming, and pitch calibration.
    \item Backend processing pipeline for ball detection, tracking, and 3D reconstruction.
    \item LBW decision engine implementing ICC Rule 36.
    \item Interactive 3D visualization of the trajectory.
    \item Local and cloud-based persistent storage of pitches and analyses.
\end{itemize}

\subsection{Limitation}

\begin{itemize}
    \item The system operates under umpire-POV camera placement; other angles may require retraining or recalibration.
    \item Accuracy degrades with heavy ball occlusion, poor lighting, or unstable camera mounting.
    \item The system is not ICC-certified and is designed for decision support rather than final authority.
    \item Monocular depth estimation remains fundamentally limited compared to multi-camera triangulation.
    \item Performance depends on proper pitch calibration; user error in marking reference points reduces decision reliability.
\end{itemize}

\section{Development Methodology}

PocketDRS followed an incremental, agile approach: requirement analysis (from literature and existing systems), proof-of-concept on calibration / detection / 3D reconstruction, iterative development of the full pipeline with continuous testing, unit and synthetic-harness validation, and deployment of the backend on Google Cloud Run with Firestore persistence alongside the mobile app on Android and iOS.

\section{Report Organization}

Chapter~2 presents the background and literature review. Chapter~3 covers system analysis and design (requirements, feasibility, ER, DFD, architecture, and the five-step algorithm). Chapter~4 details implementation and testing, including unit / system tests, synthetic validation, and real-world clips. Chapter~5 concludes with lessons learned and future recommendations. References and appendices follow.

% =====================================================================
% CHAPTER 2: BACKGROUND STUDY AND LITERATURE REVIEW
% =====================================================================
\chapter{Background Study and Literature Review}

\section{Background Study}

\textbf{Pinhole Camera Model and Intrinsics.} A smartphone camera is well approximated by the pinhole model: light passes through a single point (the lens centre) and lands on a flat sensor. The \emph{intrinsic} parameters, focal length $(f_x, f_y)$ and principal point $(c_x, c_y)$, form the camera matrix $K$ that maps a 3D point to a pixel. Zhang's checkerboard method~\cite{zhang2000} is the standard way to recover $K$. PocketDRS skips checkerboard calibration and estimates $K$ from the frame size and the typical smartphone horizontal field of view of $67^{\circ}$.

\textbf{Camera Pose (PnP).} The \emph{extrinsic} parameters $(R, t)$ place the camera in the world. The Perspective-n-Point algorithm~\cite{bradski2008} solves for them given a few known 3D world points and their pixel positions, by minimising reprojection error. PocketDRS feeds it the four tapped pitch corners.

\textbf{Homography.} A $3 \times 3$ matrix $H$ that relates two views of the same flat surface~\cite{hartley2004}. Because a cricket pitch is flat, a homography built from the four corner taps converts any pitch pixel into a pitch-plane position in metres. RANSAC~\cite{fischler1981} rejects bad correspondences.

\textbf{Projectile Motion.} Over the short flight of a delivery, air resistance is negligible and the ball obeys
\[
X(t) = X_0 + V_x t,\quad Y(t) = Y_0 + V_y t,\quad Z(t) = Z_0 + V_z t - \tfrac{1}{2} g t^2,
\]
with $g = 9.81$~m/s$^2$. This is a strong constraint: any candidate trajectory that is not parabolic in $Z$ and linear in $X$ and $Y$ cannot be a real cricket ball. The bounce is modelled as a vertical-velocity reflection scaled by a coefficient of restitution ($\approx 0.55$).

\textbf{RANSAC.} A robust estimator~\cite{fischler1981} that repeatedly samples the smallest subset needed to define a model, counts how many remaining points are inliers, and keeps the best-supported model. PocketDRS uses it twice: for the homography and for linking per-frame ball detections into one trajectory.

\textbf{Kalman Filter (for reference).} The Extended Kalman Filter is the standard frame-by-frame tracker. PocketDRS does not use it because RANSAC plus a projectile fit proved more robust to clutter and easier to explain; the EKF remains the natural extension for live tracking.

\textbf{ICC Rule 36 (LBW).} Law~36~\cite{icc} requires three things together: the ball pitches in line with the stumps, strikes the batter in line, and its projected path goes on to hit the stumps. A $\approx 25$~mm margin around the stump edge is left to the on-field umpire (``umpire's call''). PocketDRS implements all three checks and the margin in code.

\section{Literature Review}

Owens \textit{et al.}~\cite{owens2003} described the original Hawk-Eye ball-tracking system, which uses six to eight synchronized cameras and proprietary calibration to reconstruct ball trajectories to within a few millimetres. Their work is the inspiration and accuracy benchmark for PocketDRS, which trades multi-view precision for single-view affordability.

Zhang~\cite{zhang2000} introduced a flexible camera-calibration technique based on imaging a planar checkerboard, removing the need for expensive calibration rigs. This method underpins the intrinsic camera model assumed in this project.

Fischler and Bolles~\cite{fischler1981} proposed RANSAC, a robust estimator that fits a model to data containing outliers by repeatedly sampling minimal subsets and counting inliers. PocketDRS applies RANSAC both to homography estimation and to associating per-frame ball detections into a single trajectory.

Hartley and Zisserman~\cite{hartley2004} provide the standard reference on multiple-view geometry, homography, and projective reconstruction, which forms the theoretical basis for the single-view geometry used here.

Triggs \textit{et al.}~\cite{triggs2000} formalized bundle adjustment as the joint least-squares refinement of camera parameters and 3D structure; the same least-squares philosophy motivates the projectile-fit step in PocketDRS.

Ponglertnapakorn and Suwajanakorn~\cite{ponglertnapakorn2025} estimated 3D ball trajectories from a single monocular 2D track, showing that physical motion constraints make single-camera 3D ball reconstruction feasible. This is the closest prior work to PocketDRS and supports its core assumption.

Bochinski \textit{et al.}~\cite{bochinski2017} demonstrated high-quality multi-frame tracking by linking detections across frames using simple intersection-over-union association. PocketDRS follows the same detection-linking principle with a constant-acceleration motion model tailored to projectile flight.

\textbf{Synthesis.} Single-view sports analysis becomes feasible when the scene contains known geometry (the pitch) and the motion follows strong constraints (projectile motion). PocketDRS advances this area by demonstrating a complete end-to-end system combining mobile capture, cloud processing, and user-friendly visualisation.

% =====================================================================
% CHAPTER 3: SYSTEM ANALYSIS AND DESIGN
% =====================================================================
\chapter{System Analysis and Design}

\section{System Analysis}

\subsection{Requirement Analysis}

\subsubsection{Functional Requirements}

\begin{longtable}{|p{1.3cm}|p{4cm}|p{7.2cm}|}
\caption{Functional requirements of the PocketDRS system.}\\
\hline
\textbf{ID} & \textbf{Requirement} & \textbf{Description} \\
\hline
\endfirsthead
\hline
\textbf{ID} & \textbf{Requirement} & \textbf{Description} \\
\hline
\endhead
FR-01 & User Authentication & Users sign in via Firebase Authentication with email/password or social login. \\
\hline
FR-02 & Video Capture/Selection & Users can record a new video or select an existing video from the gallery. \\
\hline
FR-03 & Video Trimming & Users trim the selected video to isolate the delivery of interest. \\
\hline
FR-04 & Pitch Calibration & Users calibrate the pitch by tapping four corners and optionally stump reference points. \\
\hline
FR-05 & Calibration Validation & The system validates calibration quality and alerts users if quality is poor. \\
\hline
FR-06 & Save Pitch Config & Calibration data is saved locally and to Firestore for reuse on the same ground. \\
\hline
FR-07 & Job Submission & The user submits the trimmed video and calibration for server-side processing. \\
\hline
FR-08 & Ball Detection & The server detects ball candidates using motion and colour cues. \\
\hline
FR-09 & 3D Reconstruction & The server recovers the ball trajectory in world coordinates using physics fitting. \\
\hline
FR-10 & LBW Decision & The server evaluates and returns a decision (OUT/NOT OUT/UMPIRES CALL) with rationale. \\
\hline
FR-11 & 3D Visualization & The client displays an interactive 3D trajectory using Three.js in a WebView. \\
\hline
FR-12 & Decision History & Users view past analyses and decisions saved to Firestore. \\
\hline
\end{longtable}

The table above specifies the functional requirements of the system. As the project follows the structured approach, the system's processes are modelled by the data flow diagrams that follow, rather than by a use-case (object-oriented) diagram.

\subsubsection{Non-functional Requirements}

\begin{longtable}{|p{1.5cm}|p{3.8cm}|p{7.0cm}|}
\caption{Non-functional requirements of the PocketDRS system.}\\
\hline
\textbf{ID} & \textbf{Requirement} & \textbf{Description} \\
\hline
\endfirsthead
\hline
\textbf{ID} & \textbf{Requirement} & \textbf{Description} \\
\hline
\endhead
NFR-01 & Response Time & Processing should complete within 30 seconds for a 5-second delivery clip. \\
\hline
NFR-02 & Accuracy & Synthetic-test position error $< 80$ cm; structural correctness on real handheld video confirmed. \\
\hline
NFR-03 & Offline Support & Pitch calibration and video selection work offline; analysis requires connectivity. \\
\hline
NFR-04 & Security & User authentication and data encryption in transit; Firebase rules enforce privacy. \\
\hline
NFR-05 & Cross-Platform & App runs on Android 6.0+ and iOS 12.0+ via Flutter. \\
\hline
NFR-06 & Usability & First-time users can complete a full analysis within 5 minutes with minimal guidance. \\
\hline
NFR-07 & Maintainability & Code is modular and documented to support future enhancements. \\
\hline
NFR-08 & Data Privacy & User data is not shared with third parties; deletion requests are honored. \\
\hline
\end{longtable}

\subsection{Feasibility Analysis}

\textbf{Technical Feasibility}

The proposed system is technically feasible because it is built from mature and widely supported technologies. Flutter provides cross-platform mobile development with excellent performance. FastAPI provides lightweight and testable backend services with automatic OpenAPI documentation. OpenCV supports frame processing, tracking, and camera calibration. Firebase handles authentication and cloud persistence with real-time updates. The present implementation already demonstrates upload, calibration, server-side analysis, and result visualization on consumer devices. Although monocular video imposes accuracy limits, the required software stack is realistic for a student project and deployable on ordinary hardware.

\textbf{Operational Feasibility}

The operational workflow is simple enough for field use during testing and demonstrations. A user places a phone behind the bowler-end stumps, records a delivery, calibrates the pitch by tapping known points, trims the clip, and waits for the generated result. This procedure is repeatable, requires minimal training, and fits the needs of clubs, student projects, and coaching sessions.

\textbf{Economic Feasibility}

The project is economically feasible because it does not require specialized camera arrays, licensed broadcast systems, or custom hardware. It relies primarily on a smartphone, a tripod, and open-source libraries. Compared with professional DRS infrastructure (which costs hundreds of thousands of dollars), the expected deployment cost is extremely low, making the proposed system suitable for academic demonstration and low-budget cricket environments.

\subsection{Data Modelling: ER Diagram}

The core entities are: User (identified by Firebase UID), Pitch (a ground with calibration data), Analysis (a submitted delivery job), and LBWResult (the decision outcome). Additional entities include DeliveryVideo (the trimmed clip) and CalibrationProfile (the saved corner/stump points). The relationships are: a User owns many Pitches and submits many Analyses; each Analysis uses one Pitch, has one DeliveryVideo, and produces one LBWResult; and each Pitch defines one CalibrationProfile. Figure~\ref{fig:er} shows these entities together with their attributes and crow's-foot cardinalities.

In Firestore the data is organised under per-user collections: \texttt{users/\{uid\}} holds the user document; \texttt{users/\{uid\}/pitches/\{pitchId\}} holds saved pitch calibrations; and \texttt{users/\{uid\}/analyses/\{analysisId\}} holds completed analyses.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth,height=0.45\textheight,keepaspectratio]{figures/er_diagram.png}
    \caption{Entity--relationship diagram of the PocketDRS data model.}
    \label{fig:er}
\end{figure}

\subsection{Process Modelling: Data Flow Diagram (DFD)}

The context-level (Level 0) DFD treats the whole system as a single process. As shown in Figure~\ref{fig:dfd0}, PocketDRS receives delivery clips and calibration taps from the User, exchanges authentication and stored results with Firebase, and returns the LBW decision and 3D visualisation to the User.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{figures/dfd_level0.png}
    \caption{Context-level (Level 0) data flow diagram.}
    \label{fig:dfd0}
\end{figure}

The Level 1 DFD expands the system into four processes: (1) Capture \& Trim Delivery, handled by the mobile app; (2) Calibrate Pitch, which collects corner and stump points; (3) Analyse Delivery, where the server decodes the clip, detects the ball, tracks the trajectory, reconstructs the 3D path, and assesses LBW; and (4) Present Result, which displays the 3D trajectory and decision. These processes communicate through two data stores, the Calibration Store (D1) and the Job/Result Store (D2), as shown in Figure~\ref{fig:dfd1}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.62\textwidth,height=0.5\textheight,keepaspectratio]{figures/dfd_level1.png}
    \caption{Level 1 data flow diagram.}
    \label{fig:dfd1}
\end{figure}

\section{System Design}

\subsection{Architectural Design}

PocketDRS follows a three-tier architecture:

\begin{itemize}
    \item \textbf{Presentation Tier (Mobile):} Flutter application with video capture, calibration UI, job submission, and result visualization via WebView (Three.js).
    \item \textbf{Application Tier (Backend):} FastAPI server with REST API endpoints for uploads, job status, and result retrieval, with asynchronous job processing via background tasks.
    \item \textbf{Data Tier (Cloud):} Firebase Authentication for user identity, Firestore for document storage, Google Cloud Storage for video files, and Google Cloud Run for scalable compute.
\end{itemize}

The backend processes videos asynchronously to avoid blocking HTTP responses. Progress is tracked in Firestore and polled by the client. The overall arrangement of the three tiers is shown in Figure~\ref{fig:architecture}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.62\textwidth,height=0.5\textheight,keepaspectratio]{figures/architecture.png}
    \caption{Three-tier system architecture of PocketDRS.}
    \label{fig:architecture}
\end{figure}

\subsection{Database Schema Design}

The Firestore collections are organised as follows:

\begin{itemize}
    \item \texttt{users/\{uid\}}: User metadata (email, profile).
    \item \texttt{users/\{uid\}/pitches/\{pitchId\}}: Pitch calibration (corners, stumps, homography, quality score, creation timestamp).
    \item \texttt{users/\{uid\}/analyses/\{analysisId\}}: Analysis result (video metadata, calibration used, track points, 3D trajectory, LBW decision, diagnostic warnings).
    \item \texttt{job\_queue/\{jobId\}}: Transient job status (state, progress, error message), deleted after the result is written to analyses.
\end{itemize}

\subsection{Interface Design (UI/UX)}

The application has seven key screens: \emph{Sign In} (Firebase email/password or Google), \emph{Home} (history of past analyses with timestamps and decisions, plus a ``new analysis'' action), \emph{Calibration} (interactive pitch map for tapping the four corners and optional stump bases, with live quality feedback), \emph{Video Selection / Trim} (gallery picker with a frame-accurate trim UI), \emph{Progress} (real-time status during server processing), \emph{Result} (the 3D viewer, verdict, confidence, and reasoning), and \emph{Settings} (preferences, calibration history, and local cache). The Result Screen is the most important output surface of the app: it embeds the Three.js Hawk-Eye viewer in a WebView and renders the reconstructed 3D ball trajectory together with the LBW verdict, supporting free orbit, pan, and zoom. Figure~\ref{fig:cases} shows real renders produced by the actual viewer (\texttt{hawkeye.js}) from real pipeline output, one for each verdict class, taken from the synthetic validation runs of Section~\ref{sec:synthval}. In each render the blue line is the path before the bounce, the red dot is the bounce, the green line is the bounce-to-pad segment, and the orange line is the predicted path on to the stumps. From left to right: the predicted path strikes the stumps (OUT); clearly misses down the leg side (NOT OUT); and only just clips the stump edge inside the marginal band (UMPIRE'S CALL).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.32\textwidth]{figures/case_out.png}\hfill
    \includegraphics[width=0.32\textwidth]{figures/case_not_out.png}\hfill
    \includegraphics[width=0.32\textwidth]{figures/case_umpires_call.png}
    \caption{Result-screen renders for the three LBW verdicts.}
    \label{fig:cases}
\end{figure}

\subsection{Physical DFD}

The physical DFD in Figure~\ref{fig:physdfd} shows how the logical processes are mapped onto deployed infrastructure. The Flutter mobile application runs on the user's Android or iOS device and handles capture, trimming, and calibration; it uploads the delivery over HTTPS to the FastAPI service running on Google Cloud Run, which invokes the OpenCV/SciPy computer-vision pipeline. Job status and results are written to Cloud Firestore, while the original video files are held in Cloud Storage. The embedded Three.js WebView viewer renders the returned 3D trajectory and verdict back to the user.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.78\textwidth]{figures/physical_dfd.png}
    \caption{Physical data flow diagram of the deployed system.}
    \label{fig:physdfd}
\end{figure}

\section{Algorithm Details}

The server-side analysis is made of five steps. Each is described below in plain terms: what it is given, what it produces, and how it works. The exact mathematical formulas are standard computer-vision techniques and are available in the references; the focus here is on \emph{what} each step does and \emph{why}.

\subsection{Step 1: Finding Where the Camera Was Standing}

\textbf{Given:} the four pitch corners the user tapped on the first frame, and the known real-world size of a cricket pitch.

\textbf{Produces:} the camera's position and the direction it was pointing.

Because a cricket pitch is always the same size (about 20~m long and 3~m wide), the system can compare the shape of the pitch as it appears in the image with its known real shape. From the difference between the two, it works out where the camera must have been standing and which way it was facing. This is a well-known technique in computer vision. One known problem is that the maths has two possible answers, one with the camera above the ground and one mirrored below it; the system simply rejects the below-ground answer, since a real camera is always above the pitch.

\subsection{Step 2: Finding the Ball in Each Frame}

\textbf{Given:} each video frame, and the user-selected ball colour.

\textbf{Produces:} a list of candidate ball positions in each frame, each with a confidence score.

The ball is found in two independent ways and the results are combined:

\begin{enumerate}
    \item \textbf{By motion.} The system compares each frame with a model of the still background and keeps only the parts that are moving. It then looks for ball-shaped moving objects. Importantly, it accepts the ball in two forms: as a small \emph{round blob} (a slow or distant ball), and as a short \emph{blurred streak} (a fast ball). A fast cricket ball filmed at 30 frames per second smears into a streak rather than a clean circle, so looking only for circles would miss it.
    \item \textbf{By colour.} The system also looks for regions matching the ball's colour (for example, the red of a cricket ball).
\end{enumerate}

When a motion detection and a colour detection agree on the same spot, the system is more confident that it has found the real ball.

\paragraph{Ignoring fixed clutter.}
Before the next step, the system removes any detection that keeps appearing in the \emph{same} place throughout the clip. A real ball only passes through any given spot once or twice; a red or round object that sits still the whole time (a logo, a helmet, a bag, a bat handle) is not the ball. Any candidate that appears in more than 30\% of the frames at the same location is treated as background and discarded. This is the single most important fix for real-world handheld videos: without it, the next step would happily draw a ``ball path'' through a cluster of fixed clutter.

\subsection{Step 3: Joining the Detections into One Ball Path}

\textbf{Given:} the list of candidate ball positions from Step 2.

\textbf{Produces:} a single smooth path through the frames, plus a count of how many detections fit it and how well.

The detections from Step 2 include the real ball plus some false positives. This step finds the one consistent path among them. It takes the early frames as a starting point, tries connecting pairs of detections, and for each guess checks how many of the later detections fall close to the path a real ball would follow (a ball moves in a smooth curve, slowing slightly and curving under gravity). The guess that explains the most detections wins.

Two design choices make this reliable for phone video shot from behind the stumps. First, the ball is moving almost straight \emph{towards} the camera, so it does not appear to move much across the screen; the test therefore looks at total movement over the whole clip rather than frame-to-frame speed. Second, if the best path found only covers a tiny part of the image, it is rejected as leftover clutter, and the system honestly reports ``no clear ball trajectory'' instead of inventing a decision.

\subsection{Step 4: Rebuilding the Ball Path in Real-World 3D}

\textbf{Given:} the ball path on the image (Step 3) and the camera position (Step 1).

\textbf{Produces:} the ball's path in real-world 3D coordinates (in metres).

A single camera cannot directly see depth, so the system estimates how far away the ball is in each frame from how big it looks: a cricket ball is always 0.036~m in radius, so a smaller ball in the image means a more distant ball. Combining that distance with the camera position gives a rough 3D point for each frame. These rough points are then fitted to the physics of a thrown object: a ball in flight follows a smooth curve shaped by gravity. Fitting this physical curve smooths out the measurement noise and gives the final 3D path. The path is also anchored using the point where the ball bounces on the ground, which is the most reliable single point to pin down.

\subsection{Step 5: Making the LBW Decision (ICC Rule 36)}

\textbf{Given:} the 3D bounce point, the 3D point where the ball hit the pad, and the predicted point where the ball would have reached the stumps.

\textbf{Produces:} a decision (OUT, NOT OUT, or UMPIRE'S CALL) with a plain-language reason and a confidence score.

The decision follows the three standard LBW checks from the Laws of Cricket. The key real-world measurements it uses are the width of the stumps, the radius of the ball (0.036~m), and a small ``umpire's call'' margin of 0.025~m around the edge of the stumps.

\begin{enumerate}
    \item \textbf{Did it pitch in line?} If the ball bounced outside the line of leg stump, the decision is NOT OUT (``pitched outside leg'').
    \item \textbf{Did it hit the pad in line?} If the ball struck the pad outside the line of the stumps, the decision is NOT OUT.
    \item \textbf{Would it have hit the stumps?} The predicted path is carried on to the stumps. If it would clearly miss, the decision is NOT OUT (``missing stumps'').
\end{enumerate}

If all three checks pass, the ball would have hit the stumps. If the predicted path passes within the small 0.025~m margin of the stump edge, the result is UMPIRE'S CALL (too close to be certain); otherwise it is OUT. The confidence score is higher when the 3D path was a tight, clean fit and lower when it was noisy.

% =====================================================================
% CHAPTER 4: IMPLEMENTATION AND TESTING
% =====================================================================
\chapter{Implementation and Testing}

\section{Implementation}

\subsection{Tools Used}

The mobile client is built in \textbf{Flutter 3.8} (Dart 3.0)~\cite{flutter}, with the standard ImagePicker, VideoPlayer, and WebView plugins for media capture and the embedded 3D viewer. The backend is a \textbf{FastAPI 0.104} service~\cite{fastapi} on Python 3.11 with the computer-vision stack \textbf{OpenCV 4.8}~\cite{bradski2008}, \textbf{NumPy 1.24}, and \textbf{SciPy 1.11}~\cite{scipy2020}; the learned ball detector uses Ultralytics YOLO~\cite{ultralytics2023}. Cloud services are all Firebase / Google Cloud: \textbf{Firebase Authentication}~\cite{firebase} for user identity, \textbf{Cloud Firestore} for per-user documents, \textbf{Cloud Storage} for video files, and \textbf{Cloud Run} as the serverless compute platform. The interactive 3D viewer is built with \textbf{Three.js}~\cite{threejs} and embedded in the mobile app through a Flutter WebView. Tests run under \texttt{pytest}; logging uses the Python standard library.

\subsection{Implementation Details of Modules}

\textbf{Mobile application modules.} The mobile app has seven main modules. The \emph{Authentication} module wraps Firebase sign-in (email/password and Google) with token refresh. The \emph{Video Capture / Selection} module uses ImagePicker and the Camera plugin and supports both orientations. The \emph{Pitch Calibration} module is an interactive canvas where the user taps four pitch corners (and optionally stump bases), with live validation of calibration quality. The \emph{Video Trimming} module gives frame-accurate trim handles. The \emph{API Client} is a REST HTTP client with multipart uploads and JSON serialisation. The \emph{Local Storage} module caches pitch calibrations in SQLite and syncs to Firestore when connectivity returns. The \emph{3D Visualisation} module embeds a Three.js scene in a WebView with interactive camera controls.

\textbf{Backend processing modules.} The backend has eight modules, one per algorithm step plus orchestration. The \emph{Video Decoder} extracts frames via OpenCV with automatic rotation correction. The \emph{Ball Detector} combines motion (MOG2 background subtraction~\cite{zivkovic2004}) with HSV colour detection and ROI masking; real handheld clips additionally use a learned YOLO cricket-ball detector~\cite{redmon2016,ultralytics2023}. The \emph{Trajectory Finder} links per-frame detections into one smooth path via RANSAC with a constant-acceleration model. The \emph{Calibration Module} computes the homography from the four corner taps (OpenCV \texttt{findHomography} with RANSAC) and validates it via reprojection error. The \emph{Camera Pose Solver} runs \texttt{solvePnP} on pitch corners (and optional stump points) to recover camera extrinsics. The \emph{3D Reconstruction} module back-projects detections via depth-from-size and fits projectile motion. The \emph{LBW Decision Engine} applies the three Rule-36 checks and returns OUT / NOT OUT / UMPIRES CALL with confidence. The \emph{Job Manager} provides an asynchronous task queue with progress updates written to Firestore.

\subsection{Robustness and Edge-Case Hardening}
\label{sec:hardening}

Beyond the core algorithms, a dedicated review pass was carried out to find and fix the ways the system could crash or give a wrong answer on unusual input. Real videos and real users do not behave like clean test data, so this hardening work matters as much as the main pipeline. The fixes, in plain terms:

\begin{itemize}
    \item \textbf{Truncated video files.} Some video files claim to be longer than they really are (a wrong length field in the file header, common in trimmed or shared clips). The video reader now detects when it can no longer decode new frames and stops there, analysing the real frames it has instead of padding the analysis with repeated stale frames.
    \item \textbf{Bad pitch calibration.} If the four tapped pitch corners are all the same point, lie on a straight line, or do not form a proper four-sided shape, the calibration is now rejected immediately with a clear message, both in the app (before saving) and on the server, instead of failing confusingly much later.
    \item \textbf{Invalid numbers.} Negative pitch dimensions, an out-of-range camera field-of-view, and not-a-number values from the detector are now caught at the input boundary rather than silently flowing through the geometry and producing a meaningless result.
    \item \textbf{Network and session failures in the app.} If the job polling loop loses the connection it now stops with the real error after a few retries instead of polling silently for minutes; a genuine server-side job failure is reported immediately; an expired sign-in token produces a clean ``please sign in again'' message; and the video player resource is always released, even if loading fails midway.
    \item \textbf{3D viewer safety.} Non-finite numbers in a trajectory are coerced to safe values before being sent to the 3D view, and if the viewer's scripts never finish loading the user sees a clear error instead of a permanently blank screen.
\end{itemize}

Together these changes mean the system either produces a correct result or fails clearly and safely: it does not crash, and it does not invent a confident-but-wrong answer.

\section{Testing}

\subsection{Test Cases for Unit Testing}

The unit-test suite covers every module individually and includes both happy-path and edge-case inputs. Table~\ref{tab:unit-tests} lists all twenty cases; every case is reproducible via \texttt{pytest} on the server source tree.

\begin{longtable}{|p{0.9cm}|p{2.3cm}|p{4.0cm}|p{4.0cm}|p{0.9cm}|}
\caption{Unit test cases covering the calibration, tracking, trajectory, reconstruction, LBW, hardening, and API modules.}\label{tab:unit-tests}\\
\hline
\textbf{ID} & \textbf{Module} & \textbf{Test Case} & \textbf{Expected} & \textbf{Result} \\
\hline
\endfirsthead
\hline
\textbf{ID} & \textbf{Module} & \textbf{Test Case} & \textbf{Expected} & \textbf{Result} \\
\hline
\endhead
TC-01 & Calibration & Homography with 4 valid corners & H matrix $3 \times 3$, det$(H) \neq 0$ & PASS \\
\hline
TC-02 & Calibration & Homography reprojection error & RMS $< 5$ px on in-plane points & PASS \\
\hline
TC-03 & Calibration & PnP twofold mirror disambiguation & Camera centre $z > 0$ (above pitch) & PASS \\
\hline
TC-04 & Calibration & Degenerate corners (collinear / duplicates) & Reject with clear error message & PASS \\
\hline
TC-05 & Tracking & MOG2 motion detector on moving ball & $\geq 3$ detections in clip & PASS \\
\hline
TC-06 & Tracking & HSV colour detector on red ball & $\geq 4$ colour-matched detections & PASS \\
\hline
TC-07 & Tracking & Fixed-clutter suppression (logo / helmet) & Static red blob discarded, real ball kept & PASS \\
\hline
TC-08 & Tracking & Streak detector on motion-blurred ball & Streak accepted as ball candidate & PASS \\
\hline
TC-09 & Trajectory & RANSAC with $\geq 10$ inliers (synthetic) & Image-space RMS $< 25$ px & PASS \\
\hline
TC-10 & Trajectory & Path-length sanity check & Reject tracks covering $< 20\%$ of image width & PASS \\
\hline
TC-11 & Trajectory & Projectile-fit RMS metric & Residual $< 0.1$ m on synthetic world path & PASS \\
\hline
TC-12 & Reconstruction & Back-projection $+$ depth-from-size & 3D point within $\pm 50$ mm of ground truth & PASS \\
\hline
TC-13 & Reconstruction & Reconstruction quality gate (MAX\_FIT\_RMS\_M) & Fit RMS $> 0.75$~m discarded (no verdict) & PASS \\
\hline
TC-14 & LBW & Pitched inside leg-stump line, impact in line, hits stumps & OUT & PASS \\
\hline
TC-15 & LBW & Pitched outside leg & NOT OUT (``pitched outside leg'') & PASS \\
\hline
TC-16 & LBW & Impact outside off, off-side tolerance exceeded & NOT OUT (``impact outside off'') & PASS \\
\hline
TC-17 & LBW & Ball clearly missing stumps by 10~cm & NOT OUT (``missing stumps'') & PASS \\
\hline
TC-18 & LBW & Ball within $\pm 2.5$~cm umpires-call margin & UMPIRES CALL & PASS \\
\hline
TC-19 & Hardening & Truncated video file (wrong header length) & Stop at last decodable frame; no padding & PASS \\
\hline
TC-20 & API & JSON request / response round-trip & Payloads serialise and deserialise identically & PASS \\
\hline
\end{longtable}

\subsection{Test Cases for System Testing}

System tests exercise the full pipeline end-to-end, from the mobile client uploading a clip through to the rendered LBW verdict on the Result Screen. Table~\ref{tab:system-tests} lists every scenario; all twelve cases pass on the deployed build.

\begin{longtable}{|p{0.9cm}|p{2.4cm}|p{4.6cm}|p{4.0cm}|p{0.8cm}|}
\caption{System (end-to-end) test cases.}\label{tab:system-tests}\\
\hline
\textbf{ID} & \textbf{Scenario} & \textbf{Test Description} & \textbf{Expected Result} & \textbf{Res.} \\
\hline
\endfirsthead
\hline
\textbf{ID} & \textbf{Scenario} & \textbf{Test Description} & \textbf{Expected Result} & \textbf{Res.} \\
\hline
\endhead
ST-01 & Server Health & GET /health endpoint & 200 OK with build info and uptime & PASS \\
\hline
ST-02 & Authentication & Firebase email/password sign-in then authenticated API call & Token returned; subsequent call accepted & PASS \\
\hline
ST-03 & Calibration Save & POST /calibrations with four corners & Calibration saved to Firestore; ID returned & PASS \\
\hline
ST-04 & Clean Delivery (OUT) & End-to-end synthetic clip pitched and hitting stumps & Decision OUT with explanation and 3D path & PASS \\
\hline
ST-05 & Clean Delivery (NOT OUT) & Synthetic clip pitched outside leg & NOT OUT with ``pitched outside leg'' & PASS \\
\hline
ST-06 & Clean Delivery (Umpire's Call) & Synthetic clip whose projected path just clips the stump edge & UMPIRES CALL with confidence value & PASS \\
\hline
ST-07 & No Trajectory & Clip with no moving ball at all & INVALID\_REQUEST with diagnostic warning (no fake verdict) & PASS \\
\hline
ST-08 & Real Clip (Bad Conditions) & Handheld clip with low camera, netting, and clutter (test1) & No-trajectory warning (refuses to invent a verdict) & PASS \\
\hline
ST-09 & Real Clip (Good Conditions) & Handheld 60~fps off-spin clip (test3, YOLO detector) & 25-point clean track, plausible 3D arc & PASS \\
\hline
ST-10 & Truncated Video & Clip whose header overstates length & Pipeline stops at last decodable frame; succeeds on real frames & PASS \\
\hline
ST-11 & Session Recovery & App polling job while network drops twice then recovers & Polling resumes; result delivered without duplicate jobs & PASS \\
\hline
ST-12 & 3D Viewer Safety & Trajectory containing non-finite values & Coerced to safe values; viewer renders or shows reload prompt & PASS \\
\hline
\end{longtable}

\subsection{Synthetic Validation}
\label{sec:synthval}

A controlled synthetic harness was built to validate the pipeline against delivery scenarios with known ground truth. OpenCV renders a textured pitch trapezoid, crease lines, and a red ball following a physically simulated projectile (gravity $9.81$ m/s$^2$, vertical restitution $0.55$). The synthetic camera is placed behind the striker at $(-3.5, 0, 1.8)$ m looking at $(10, 0, 0)$, with intrinsics $f_x = f_y = 900$, $c_x = 540$, $c_y = 960$ giving a $61.9^{\circ}$ horizontal field of view. Output is $1080 \times 1920$ portrait at 60 fps, matching a phone shot.

The harness sweeps three release speeds (medium-pace $18$, $24$, and $30$ m/s) across eleven release-line presets (clearly wide of off, off-stump line, middle stump, leg-stump line, outside leg, and going-down/wide deliveries) and two release angles, producing $66$ ground-truth-labelled scenarios.

\paragraph{Per-stage results.}
\begin{itemize}
    \item \textbf{Camera calibration.} PnP reprojection error $0.00$ px on every scenario; the recovered camera centre matches ground truth exactly to three decimal places. The fix that resolved this was disambiguating the twofold planar-PnP mirror by requiring the camera to be above the pitch.
    \item \textbf{Ball detection and trajectory association.} The combined MOG2 motion plus HSV-red colour detector finds $60$--$130$ candidates per scenario; the RANSAC association recovers $60$--$70$ inliers per delivery (typically $\sim 95\%$ of in-pitch detections), with image-space RMS in the $10$--$25$ px range.
    \item \textbf{3D reconstruction.} The bundle-adjusted projectile fit, augmented by an image-space bounce anchor that back-projects the bounce pixel onto the calibrated pitch plane, returns smooth parabolic trajectories that traverse the full pitch length. Physical bounds on the optimiser (release height $\in [0, 3.5]$~m, downward $v_z$, capped forward speed) eliminate the unphysical local minima that earlier bundle adjustment occasionally fell into.
    \item \textbf{LBW decisions.} Across the 66-scenario sweep the decision engine returns the correct verdict on $53/66$ deliveries ($80.3\%$ overall). Per class:
    \begin{itemize}
        \item OUT: $16/16$ ($100\%$).
        \item NOT OUT: $36/44$ ($81.8\%$).
        \item UMPIRE'S CALL: $1/6$ ($16.7\%$, see discussion below).
    \end{itemize}
\end{itemize}

\paragraph{Sample rendered decisions.}
Figure~\ref{fig:cases} (shown earlier, in the Interface Design section) gives real Hawk-Eye viewer renders of one scenario from each verdict class in this sweep: a leg-stump-line delivery (OUT), a delivery going down the leg side (NOT OUT), and a marginal leg-stump delivery (UMPIRE'S CALL). In each, the recovered blue trajectory starts at the bowler's release point, descends, bounces on the pitch, and the verdict follows from where the path meets the stump plane.

\paragraph{Where accuracy degrades.}
All $13$ failed verdicts are concentrated in two regimes that are genuinely difficult for monocular geometry:
\begin{enumerate}
    \item Fast deliveries ($30$ m/s) tracked over very few frames, where radius-from-size noise dominates depth resolution; and
    \item ``Leg marginal'' close calls where the recovered impact-Y differs from ground truth by $5$--$20$ cm and shifts the verdict from UMPIRE'S CALL to NOT OUT.
\end{enumerate}
Both reflect the inherent precision floor of single-camera depth-from-size on a ball travelling along the camera optical axis. Real Hawk-Eye uses six to eight synchronised cameras for exactly this reason; this project documents the monocular feasibility envelope rather than claiming broadcast-grade accuracy. Within the OUT class the system matches ground truth perfectly, which is the most consequential decision for an umpire to support.

\subsection{Real-World Validation}

The pipeline was also run on a real cricket video, a handheld $1080 \times 1920$, 30 frames-per-second clip recorded at a practice-nets facility. The camera was placed correctly, behind the bowler-end stumps looking down the pitch (the umpire's point of view). This test was very useful: it did not produce a good ball track, and the reasons why are important lessons for anyone using the system. Figure~\ref{fig:real_validation} shows the opening frame (umpire-POV camera behind the bowler-end stumps) and a frame from the bowler's delivery stride, where his body fills most of the view at the exact moment the ball is bowled.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{figures/real_video_frame0.jpg}
    \hfill
    \includegraphics[width=0.4\textwidth]{figures/real_video_action.jpg}
    \caption{Frames from the real-world test clip.}
    \label{fig:real_validation}
\end{figure}

\paragraph{What went wrong, in plain terms.}
Four separate problems were found, and only one of them was a software bug:

\begin{enumerate}
    \item \textbf{The video's length information was wrong.} The video file said it was 7 seconds long, but only 3.25 seconds of it could actually be read (98 frames). This is common with clips that have been trimmed or shared online; the editor does not always rewrite the file header. The video content itself was fine; only the header was wrong. This \emph{was} a real software gap: the pipeline trusted the header and tried to read frames that did not exist, padding the analysis with repeated stale frames. It was fixed (see Section~\ref{sec:hardening}).
    \item \textbf{The camera was too low.} The camera was behind the stumps (correct direction) but only at chest height. The bowler runs up and delivers right past the camera, so his body blocks 20--36\% of the view down the pitch, and it blocks it during exactly the moment the ball is in flight. Professional ball-tracking cameras are mounted high up, above head height, so they can see over the bowler. A low camera simply cannot see the ball.
    \item \textbf{The clip was filmed through netting.} Green practice-net mesh crosses the whole frame, including the path the ball travels.
    \item \textbf{The background was busy.} Swaying nets, trees, and two moving people (the bowler and the batter) create dozens of small moving objects every frame. The real ball is just one faint, low-contrast, motion-blurred object lost among them.
\end{enumerate}

\paragraph{Why this is a good result, not a failure.}
Because of the fixed-clutter removal and the path-length check added to the detector (Steps~2 and~3), the pipeline did the \emph{safe} thing: it reported ``no clear ball trajectory found'' instead of inventing a confident-but-wrong decision from background clutter. An earlier version of the system, before these checks, would have produced a fake verdict, the dangerous behaviour for any decision-support tool. A system that honestly says ``I cannot see the ball in this clip'' is trustworthy; one that guesses is not.

\paragraph{What a good test video looks like.}
The lesson from this clip is a simple checklist for recording a usable delivery: mount the phone \emph{high} (above head height) behind the bowler-end stumps so the bowler's body does not block the pitch; keep a clear line of sight with no netting between the camera and the pitch; record one continuous delivery with no edits or text overlays; and use the in-app tap calibration so the four pitch corners are marked precisely. With those conditions met (the same conditions the synthetic validation already simulates), the pipeline has everything it needs.

\subsection{Real-World Validation: A Successfully Tracked Delivery}
\label{sec:realval_success}

The checklist above was then put to the test on a second real clip (\texttt{test3.mp4}): a $1080 \times 1920$, 60 frames-per-second handheld recording of an off-spin delivery at an indoor practice-nets facility, filmed from behind the bowler looking down the pitch. The decisive difference from the first clip is line of sight: an off-break is \emph{flighted}, so the ball loops up above the bowler's arm and the net line and stays visible against the open background for its whole descent onto the batter. This is exactly the condition the failure clip lacked, and it lets the pipeline run end-to-end on genuine phone footage.

\paragraph{Tracking result.}
The learned YOLO cricket-ball detector~\cite{redmon2016,ultralytics2023} was run on every readable frame; as with the first clip the container over-reported its length (it claims $290$ frames / $4.8$\,s but only $101$ frames decode), and the truncation guard from Section~\ref{sec:hardening} stopped the run cleanly at the last real frame instead of padding with stale frames. The RANSAC constant-acceleration association then recovered a single clean trajectory:

\begin{itemize}
    \item \textbf{25 ball positions} along one continuous arc, from the point of release (frame~33, $t = 550$\,ms) to arrival at the batter (frame~64, $t = 1067$\,ms): a $517$\,ms flight tracked at 60\,fps.
    \item \textbf{22 RANSAC inliers} out of $33$ surviving candidates, with an image-space fit RMS of \textbf{14.4\,px} on a 1080p frame; the ball sweeps $341 \times 570$\,px across the image.
    \item A static red false-positive that recurred at one fixed pixel location for most of the clip was correctly removed by the fixed-clutter suppression (Step~3) before association, so it never contaminated the fit.
\end{itemize}

Figure~\ref{fig:test3_path} shows the recovered path on a mid-flight frame (left) and the same delivery lifted to a 3D arc (right). The green marker is the bowler's release point, the colour-graded markers trace the ball down through its arc, and the red marker is arrival at the batter's pads, the line on which an LBW judgement would be made.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.40\textwidth]{figures/test3_path_overlay.png}
    \hfill
    \includegraphics[width=0.56\textwidth]{figures/test3_3d_path.png}
    \caption{Left: full tracked ball path on \texttt{test3.mp4}, 25 positions recovered by YOLO + RANSAC at 60\,fps. Right: depth-from-apparent-size 3D arc; release depth $1.41$\,m to arrival depth $2.39$\,m from the camera.}
    \label{fig:test3_path}
\end{figure}

\paragraph{3D reconstruction and its honest limit.}
The lift to 3D uses depth-from-apparent-size: the known physical radius of a cricket ball ($\approx 36$\,mm) against its measured pixel radius gives the ball's distance from the camera in each frame (estimated intrinsics $f_x = f_y \approx 816$\,px for the $67^{\circ}$ field of view). The arc has the correct \emph{shape}, a flighted off-break that loops and descends onto the batter. Its absolute \emph{scale}, however, is monocular-ambiguous: a single camera on a short coaching net cannot pin down true distance, so the recovered depths ($1.4$ to $2.4$\,m) and the implied average speed ($\approx 20$\,km/h) are order-of-magnitude estimates, not calibrated measurements. This is the same single-camera depth-from-size precision floor identified in the synthetic sweep (Section~\ref{sec:synthval}), and is precisely why broadcast Hawk-Eye uses six to eight synchronised cameras.

\paragraph{Why this clip matters.}
Taken together, the two real clips bracket the system's real-world envelope. When the ball is hidden (low camera, netting, clutter) the pipeline refuses to invent a verdict; when the ball stays in view, the same unchanged pipeline recovers a clean full-flight 2D track and a plausibly-shaped 3D arc end-to-end on real handheld phone footage, while remaining honest about the metric precision a single phone camera can and cannot deliver. That combination, a usable track when conditions are met and a safe refusal when they are not, is the behaviour a decision-support tool should have.

% =====================================================================
% CHAPTER 5: CONCLUSION AND FUTURE RECOMMENDATIONS
% =====================================================================
\chapter{Conclusion and Future Recommendations}

\section{Conclusion}

PocketDRS successfully demonstrates that a low-cost, single-phone-camera cricket decision-review system is technically feasible and operationally practical. The project delivers a complete end-to-end workflow from video capture to explainable LBW decision, integrating computer vision, 3D reconstruction, physics modeling, and domain-specific decision logic.

Key achievements include:
\begin{enumerate}
    \item A user-guided pitch calibration workflow that requires no specialized equipment or training.
    \item A combined motion and colour ball detector robust to real-world noise and false positives.
    \item RANSAC-based trajectory association that extracts a single ball path from cluttered per-frame detections.
    \item Physics-constrained monocular 3D reconstruction with $0.5$--$0.8$ m position accuracy on synthetic ground-truth trajectories.
    \item An ICC Rule 36 compliant LBW decision engine producing pitching, impact, and hitting-stump verdicts (out/not out/umpire's call) on both synthetic and real-world inputs.
    \item A mobile application and cloud backend demonstrating practical deployment with user-friendly visualization.
\end{enumerate}

The system is not ICC-certified and does not match the 3--5 mm accuracy of Hawk-Eye, but it is transparent, affordable, and suitable for decision support in low-resource cricket environments. The modular architecture enables future enhancements such as learned ball detection, multi-camera fusion, and on-device inference.

\section{Lesson Learnt/Outcome}

\begin{itemize}
    \item \textbf{Strong constraints are powerful:} physics-based fitting beat purely data-driven approaches in recovering clean 3D trajectories from noisy 2D detections.
    \item \textbf{User calibration is critical:} small errors in marking pitch corners produce large errors at the far end; automatic landmark detection would reduce user burden.
    \item \textbf{Monocular depth is hard:} depth-from-size is noisy when the ball is small; physics fitting mitigates this but the problem stays under-constrained. Multi-camera fusion is the natural next step.
    \item \textbf{Agile development works:} incremental prototyping with regular testing enabled early course correction. Validating each module independently before integration paid off.
    \item \textbf{Fail-safe behaviour matters:} for a decision-support tool, a clear ``no verdict'' is more trustworthy than a confidently-wrong one.
\end{itemize}

\section{Future Recommendations}

\begin{enumerate}
    \item \textbf{Multi-camera fusion:} stereo or two-phone triangulation removes depth-from-size noise entirely.
    \item \textbf{Learned ball detection by default:} promote the YOLO detector~\cite{redmon2016,ultralytics2023} introduced for the real-world clip (Section~\ref{sec:realval_success}) to the default for all inputs.
    \item \textbf{Automatic landmark detection:} detect stumps, crease lines, and pitch edges via keypoint detection rather than user taps.
    \item \textbf{Real-time streaming:} live in-match feedback instead of post-clip analysis.
    \item \textbf{On-device inference:} detection and lightweight decision logic on the phone; heavier 3D and LBW logic stays on the server.
    \item \textbf{Improved bounce model:} include air resistance, spin, and energy-loss physics.
    \item \textbf{Statistical confidence intervals:} propagate uncertainty through the pipeline.
    \item \textbf{User studies:} field testing with umpires and players to validate UI/UX choices.
\end{enumerate}

% =====================================================================
% REFERENCES (IEEE)
% =====================================================================
\addcontentsline{toc}{chapter}{References}
\begin{thebibliography}{99}

\bibitem{owens2003} N. Owens, C. Harris, and C. Stennett, ``Hawk-eye tennis system,'' in \textit{Proc. Int. Conf. Visual Information Engineering (VIE)}, Surrey, U.K., Jul. 2003, pp. 182--185.

\bibitem{zhang2000} Z. Zhang, ``A flexible new technique for camera calibration,'' \textit{IEEE Trans. Pattern Anal. Mach. Intell.}, vol. 22, no. 11, pp. 1330--1334, Nov. 2000.

\bibitem{fischler1981} M. A. Fischler and R. C. Bolles, ``Random sample consensus: A paradigm for model fitting with applications to image analysis and automated cartography,'' \textit{Commun. ACM}, vol. 24, no. 6, pp. 381--395, Jun. 1981.

\bibitem{triggs2000} B. Triggs, P. F. McLauchlan, R. I. Hartley, and A. W. Fitzgibbon, ``Bundle adjustment: A modern synthesis,'' in \textit{Vision Algorithms: Theory and Practice} (Lecture Notes in Computer Science, vol. 1883). Berlin, Germany: Springer, 2000, pp. 298--372.

\bibitem{hartley2004} R. Hartley and A. Zisserman, \textit{Multiple View Geometry in Computer Vision}, 2nd ed. Cambridge, U.K.: Cambridge Univ. Press, 2004.

\bibitem{ponglertnapakorn2025} P. Ponglertnapakorn and S. Suwajanakorn, ``Where is the ball: 3D ball trajectory estimation from 2D monocular tracking,'' in \textit{Proc. IEEE/CVF Conf. Computer Vision and Pattern Recognition Workshops (CVPRW)}, 2025, pp. 6168--6177.

\bibitem{bradski2008} G. Bradski and A. Kaehler, \textit{Learning OpenCV: Computer Vision with the OpenCV Library}. Sebastopol, CA, USA: O'Reilly Media, 2008.

\bibitem{bochinski2017} E. Bochinski, V. Eiselein, and T. Sikora, ``High-speed tracking-by-detection without using image information,'' in \textit{Proc. IEEE Int. Conf. Advanced Video and Signal Based Surveillance (AVSS)}, Lecce, Italy, Aug.--Sep. 2017, pp. 1--6.

\bibitem{zivkovic2004} Z. Zivkovic, ``Improved adaptive Gaussian mixture model for background subtraction,'' in \textit{Proc. 17th Int. Conf. Pattern Recognition (ICPR)}, Cambridge, U.K., Aug. 2004, vol. 2, pp. 28--31.

\bibitem{redmon2016} J. Redmon, S. Divvala, R. Girshick, and A. Farhadi, ``You only look once: Unified, real-time object detection,'' in \textit{Proc. IEEE Conf. Computer Vision and Pattern Recognition (CVPR)}, Las Vegas, NV, USA, Jun. 2016, pp. 779--788.

\bibitem{ultralytics2023} G. Jocher, A. Chaurasia, and J. Qiu, ``Ultralytics YOLOv8.'' Ultralytics, 2023. Accessed: May 2026. [Online]. Available: \url{https://github.com/ultralytics/ultralytics}

\bibitem{scipy2020} P. Virtanen \textit{et al.}, ``SciPy 1.0: Fundamental algorithms for scientific computing in Python,'' \textit{Nature Methods}, vol. 17, no. 3, pp. 261--272, Mar. 2020.

\bibitem{fastapi} S. Ram\'{\i}rez, ``FastAPI: Modern, fast (high-performance) web framework for building APIs with Python.'' Tiangolo. Accessed: May 2026. [Online]. Available: \url{https://fastapi.tiangolo.com}

\bibitem{flutter} Google, ``Flutter: Build apps for any screen.'' Google. Accessed: May 2026. [Online]. Available: \url{https://flutter.dev}

\bibitem{threejs} R. Cabello and contributors, ``Three.js: JavaScript 3D library.'' Accessed: May 2026. [Online]. Available: \url{https://threejs.org}

\bibitem{firebase} Google, ``Firebase: App development platform.'' Google. Accessed: May 2026. [Online]. Available: \url{https://firebase.google.com}

\bibitem{icc} Marylebone Cricket Club, ``The Laws of Cricket (2017 Code, 3rd edition, 2022): Law 36 -- Leg Before Wicket.'' MCC. Accessed: May 2026. [Online]. Available: \url{https://www.lords.org/mcc/the-laws-of-cricket}

\end{thebibliography}

% =====================================================================
% APPENDICES
% =====================================================================
\clearpage
\chapter*{Appendices}
\addcontentsline{toc}{chapter}{Appendices}

\section*{Appendix A: REST API Summary}

The mobile client communicates with the backend through a small REST surface. Each request and response payload is JSON. The principal fields are summarised below.

\textbf{Analysis request} (\texttt{POST /analyses}) carries: \texttt{user\_id} (Firebase UID), \texttt{video\_path} (Cloud Storage URI), \texttt{pitch\_corners\_px} (the four tapped corners as \{x, y\} pairs), optional \texttt{stump\_bases\_px} and \texttt{stump\_tops\_px}, \texttt{trim\_start\_ms}, \texttt{trim\_end\_ms}, and \texttt{ball\_color} (\texttt{"red"} or \texttt{"white"}).

\textbf{Analysis response} reports: \texttt{job\_id}, \texttt{status}, \texttt{video} metadata (duration, fps), the recovered \texttt{calibration} (intrinsics $K$, rotation \texttt{rvec}, translation \texttt{tvec}, reprojection error in pixels), the \texttt{track} (per-frame image points, RANSAC inlier count, image-space RMS), the \texttt{world\_trajectory} (3D points plus the projectile-fit parameters and metric RMS), the detected \texttt{events} (bounce and impact in pitch coordinates), the \texttt{lbw} decision (verdict, plain-English reason, the three Rule-36 check booleans, and the predicted strike coordinates at the stump plane), and a \texttt{diagnostics} block with warnings and a log identifier.

\section*{Appendix B: Application Screenshots}

\renewcommand{\thefigure}{B.\arabic{figure}}
\setcounter{figure}{0}

\begin{figure}[H]
    \centering
    \includegraphics[height=0.32\textheight,keepaspectratio]{figures/app_calibration.png}
    \hspace{6pt}
    \includegraphics[height=0.32\textheight,keepaspectratio]{figures/app_result_out.png}
    \caption{Left: pitch calibration recovering a valid above-ground camera pose ($8.6$\,px reprojection error). Right: result screen with the reconstructed trajectory and an OUT verdict.}
    \label{fig:app_screens}
\end{figure}

Figure~\ref{fig:app_screens} shows the calibration step on a real practice-net clip (left), and the 3D result viewer where the recovered ball path descends, bounces, and the predicted continuation strikes the stumps for an OUT verdict (right).

\section*{Appendix C: LBW Decision Engine (Pseudocode)}

The Rule-36 decision engine in \texttt{server/app/pipeline/process\_job.py} is reproduced below in concise pseudocode. \texttt{BALL\_RADIUS\_M} is $0.036$, \texttt{WICKET\_HALF\_WIDTH\_M} is half the stump width, \texttt{WICKET\_GUARD\_M} adds one ball radius, and the umpire's-call bands are one ball radius horizontally and one ball diameter vertically (to absorb the monocular depth noise floor).

\begin{lstlisting}[language=Python]
def decide_lbw(bounce, impact, pred_y, pred_z, stump_x_m):
    checks = {"pitching": True, "impact": True, "hitting": False}
    reasons = []
    # 1. Pitched in line: allow one stump width outside leg.
    if bounce and abs(bounce.y) > WICKET_HALF_WIDTH_M + 0.1143:
        checks["pitching"] = False
        reasons.append(f"Pitched outside leg ({bounce.y*100:+.1f}cm)")
    # 2. Impact in line: must be inside stump line + ball radius (off side).
    if impact and impact.y > WICKET_GUARD_M + 0.05:
        checks["impact"] = False
        reasons.append(f"Impact outside off ({impact.y*100:+.1f}cm)")
    # 3. Hitting stumps: per-axis umpire's-call bands.
    if pred_y is not None and pred_z is not None:
        hits_h = abs(pred_y) <= WICKET_GUARD_M
        hits_v = 0.0 <= pred_z <= STUMP_HEIGHT_M + BALL_RADIUS_M
        if hits_h and hits_v:
            checks["hitting"] = True
            in_band = (WICKET_GUARD_M - abs(pred_y) <= BALL_RADIUS_M or
                       (STUMP_HEIGHT_M + BALL_RADIUS_M) - pred_z <= 2 * BALL_RADIUS_M or
                       pred_z <= 2 * BALL_RADIUS_M)
    decision = ("umpires_call" if in_band else "out") if all(checks.values()) else "not_out"
    return {"decision": decision, "checks": checks,
            "reason": "; ".join(reasons) or "Hitting stumps"}
\end{lstlisting}

\clearpage
\addcontentsline{toc}{chapter}{Project Log Book}

% Filled log-book entry. Each call renders one page.
% Arguments: {meetingNo}{date}{startTime}{finishTime}{discussionList}{achievementList}{problemsList}{nextList}
\newcommand{\logsheet}[8]{%
\begingroup\singlespacing\setlength{\parskip}{0pt}%
\begin{center}\textbf{\fontsize{13}{16}\selectfont Project Log Book Entry Sheet}\end{center}
\vspace{0.15cm}
\noindent\begin{minipage}[t]{0.52\textwidth}\textbf{Meeting No.:} #1\end{minipage}\hfill\begin{minipage}[t]{0.42\textwidth}\textbf{Date:} #2\end{minipage}\par
\vspace{4pt}\noindent\begin{minipage}[t]{0.52\textwidth}\textbf{Start Time:} #3\end{minipage}\hfill\begin{minipage}[t]{0.42\textwidth}\textbf{Finish Time:} #4\end{minipage}\par
\vspace{8pt}\noindent\textbf{Discussion Topics:}\par
#5
\vspace{4pt}\noindent\textbf{Achievements:}\par
#6
\vspace{4pt}\noindent\textbf{Problems (if any):}\par
#7
\vspace{4pt}\noindent\textbf{Task for next meeting:}\par
#8
\vspace{6pt}\noindent\rule{\textwidth}{0.4pt}\par
\vspace{4pt}\noindent\begin{minipage}[t]{0.55\textwidth}Student Name: Niraj Kafle\hspace{8pt}Signature: \dotfill\end{minipage}\hfill\begin{minipage}[t]{0.4\textwidth}\centering Mentor Signature: \dotfill\end{minipage}\par
\vspace{18pt}\noindent\rule{\textwidth}{1.2pt}\par\vspace{14pt}
\endgroup}

\logsheet{01}{15/11/2025}{10:00 AM}{11:00 AM}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item Introduction to BIT 7th-semester final-year project requirements and timeline.
\item Discussion of project scope: must be original, non-trivial, technically substantial.
\item Brainstormed initial problem areas: cricket analytics, agricultural sensing, mobile health.
\item Reviewed final-year report format and IEEE referencing expectations.
\end{enumerate}}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item Project roadmap and key deadlines understood.
\item Three candidate problem areas shortlisted for the next meeting.
\item Received the BIT project syllabus and report sample.
\end{enumerate}}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item None at this stage.
\end{enumerate}}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item Refine the three problem areas into concrete project ideas with scope.
\item Draft a one-page problem statement for each.
\end{enumerate}}

\logsheet{02}{20/12/2025}{10:00 AM}{11:15 AM}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item Presented three project ideas with problem statements and scope.
\item Selected ``PocketDRS: low-cost single-camera LBW Decision Review System for cricket''.
\item Discussed feasibility: smartphone camera, FastAPI backend, OpenCV pipeline.
\item Guidance on literature review (Hawk-Eye, monocular tracking, RANSAC, PnP).
\end{enumerate}}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item Project title approved by supervisor.
\item Problem statement and primary objective finalised.
\item Reference list of seed papers compiled (Owens 2003, Zhang 2000, Fischler 1981, Hartley 2004).
\end{enumerate}}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item None.
\end{enumerate}}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item Complete literature-review draft in IEEE format.
\item Draft functional and non-functional requirements.
\item Start system design (DFD, ER).
\end{enumerate}}

\logsheet{03}{21/02/2026}{10:00 AM}{11:30 AM}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item Reviewed literature review and requirements tables (FR-01 to FR-12, NFR-01 to NFR-08).
\item Confirmed the technology stack: Flutter (mobile), FastAPI (backend), OpenCV / SciPy (vision), Firebase / Cloud Run (cloud).
\item Mandated the structured approach for design: ER + DFD Level 0 / Level 1 + Physical DFD only (no OO diagrams).
\item Discussed mid-defense expectations and synthetic-validation harness design.
\end{enumerate}}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item Chapters 1 and 2 of the report completed and approved.
\item ER diagram and DFD Level 0 and Level 1 drafted in draw.io.
\item Synthetic-validation harness implemented in OpenCV (textured pitch, projectile ball).
\end{enumerate}}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item Initial PnP solutions occasionally returned the below-ground mirror; fixed by rejecting any pose with negative camera height.
\item Early synthetic deliveries produced occasional unphysical bundle-adjustment minima; added bounds on release height and forward speed.
\end{enumerate}}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item Complete Chapter 3 (system design) and submit project proposal.
\item Prepare a live demonstration of the synthetic pipeline for the mid defense.
\end{enumerate}}

\logsheet{04}{14/03/2026}{2:00 PM}{3:30 PM}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item Mid-defense presentation to supervisor and panel (15 minutes plus Q\&A).
\item Live demonstration: synthetic pipeline running end-to-end with all three verdict classes.
\item Q\&A: depth-from-size accuracy floor, fail-safe behaviour, real-world readiness.
\item Action items received from the panel.
\end{enumerate}}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item Mid-defense cleared.
\item Synthetic harness reported: 53 / 66 correct verdicts (80.3\%); OUT class 16 / 16 (100\%).
\item Panel approved the structured-design approach and the rule-based LBW engine.
\end{enumerate}}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item Panel asked for evidence on real handheld phone video, not only on the synthetic harness.
\item Panel asked for more rigorous edge-case testing (truncated videos, bad calibration, no ball).
\item Panel asked for additional citations (MOG2 background subtraction, learned detection).
\end{enumerate}}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item Record and process a real handheld phone clip; document outcome honestly.
\item Hardening pass: catch truncated videos, degenerate corners, NaN values, network failures.
\item Expand the unit-test and system-test tables; add cites for MOG2 and YOLO.
\end{enumerate}}

\logsheet{05}{20/05/2026}{10:00 AM}{12:00 PM}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item Final report walk-through: every chapter reviewed.
\item Format mandate: 16/14/12-pt bold headings, centred chapter titles, bold-centred captions, IEEE numbered citations, abstract 150 to 165 words.
\item Reviewed real-world validation: bad clip (no trajectory returned, correctly) and successful clip (test3.mp4, 25-point clean track via YOLO).
\item Final-defense preparation: presentation deck, demo plan, viva questions.
\end{enumerate}}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item Final report draft accepted; only minor formatting fixes outstanding.
\item Defense slide deck approved at 22 slides (structured order: ER, DFD, then algorithm and results).
\item Defense-preparation guide produced with full Q\&A and a technical glossary.
\end{enumerate}}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item Abstract was slightly over the word limit and was trimmed to 160 words.
\item LaTeX figure placement initially drifted across sections; resolved using the float \texttt{[H]} option.
\end{enumerate}}{%
\begin{enumerate}\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\topsep}{0pt}
\item Submit the final report and the defense slide deck.
\item Rehearse the demo on the project laptop and have backup renders ready.
\item Carry printed copies of the report, the IEEE reference list, and the defense-prep guide.
\end{enumerate}}

\end{document}
